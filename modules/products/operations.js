import { addProduct, removeProduct, updateProduct, getProduct, resetProduct} from './actions';

export default {
    addProduct,
    removeProduct, 
    updateProduct,
    getProduct,
    resetProduct
};
